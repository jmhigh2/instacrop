function create_contact(idNum) {
  // magic creation of contact
  var createId = "create" + idNum;
  var updateId = "update" + idNum;
  var createButton = document.getElementById(createId);
  var updateButton = document.getElementById(updateId);

  createButton.style.display = "";
  updateButton.style.display = "";
}
